## 1. Схема данных

- [ ] 1.1 Завести сущности пользователя и сессии и их конфигурацию в
      `MessengerDbContext`: уникальные индексы на `login` и `email`, индекс на
      хеш идентификатора сессии, ссылка сессии на пользователя с удалением
      каскадом. Проверка - `dotnet build` зеленый.
- [ ] 1.2 Создать миграцию EF Core с таблицами `users` и `sessions`. Проверка -
      `dotnet ef migrations add` отработал, накат и откат на поднятой БД из
      `docker-compose.yml` проходят без ошибок.
- [ ] 1.3 Научить тесты работать с реальной БД: накат миграций перед прогоном и
      уникальные логины на тест, чтобы прогоны не мешали друг другу. Проверка -
      тест, создающий пользователя напрямую через контекст, проходит дважды
      подряд.

## 2. Пароли и сессии

- [ ] 2.1 Завести хеширование пароля на `PasswordHasher<T>` из shared
      framework. Проверка - unit-тесты: пароль не совпадает с хранимым
      значением, одинаковые пароли дают разные значения, проверка верного
      пароля успешна, неверного - нет.
- [ ] 2.2 Завести выдачу и проверку сессии: 32 случайных байта, base64url
      клиенту, SHA-256 в таблицу, срок жизни из конфигурации. Проверка -
      unit-тесты: выданная сессия признается, завершенная и истекшая - нет,
      неизвестный идентификатор не признается.
- [ ] 2.3 Вынести срок жизни сессии и атрибуты cookie в конфигурацию со
      значениями по умолчанию для локального стенда. Проверка - тест на
      значения по умолчанию и на их переопределение конфигурацией.

## 3. Аутентификация запроса

- [ ] 3.1 Реализовать схему authentication, которая читает cookie сессии и
      поднимает текущего пользователя, и подключить ее в `Program.cs`.
      Проверка - e2e: запрос к защищенному действию с валидной cookie проходит,
      без cookie - нет.
- [ ] 3.2 Убедиться, что `[Authorize]` отвечает `401` с пустым телом, не ломая
      `{ reason }` для остальных кодов. Проверка - e2e на пустое тело `401` и
      на сохранившийся формат тела `404`.

## 4. Регистрация и вход

- [ ] 4.1 Реализовать `POST /auth/signup`: создание пользователя, ответ
      `{ id }`, `400` на занятый логин и на занятый email через нарушение
      уникального индекса. Проверка - e2e по сценариям спеки, включая
      отсутствие cookie в ответе.
- [ ] 4.2 Реализовать валидацию полей регистрации по контракту - обязательность
      всех шести полей, regex `email` и `phone` - с телом `{ reason }`.
      Проверка - e2e на каждое нарушение.
- [ ] 4.3 Реализовать `POST /auth/signin`: проверка пароля, установка cookie
      сессии, `200` с пустым телом, `400` с общим текстом на неверные учетные
      данные, замена прежней сессии при повторном входе. Проверка - e2e по
      сценариям спеки.

## 5. Восстановление сессии и выход

- [ ] 5.1 Реализовать `GET /auth/user`: профиль текущего пользователя,
      `display_name` и `avatar` ключами со значением `null`, `401` с пустым
      телом без сессии и с недействительной cookie. Проверка - e2e по сценариям
      спеки.
- [ ] 5.2 Реализовать `POST /auth/logout`: завершение сессии, снятие cookie,
      `200` с пустым телом в том числе без сессии. Проверка - e2e, включая
      `401` на `GET /auth/user` с той же cookie после выхода.
- [ ] 5.3 Сверить ответы всех четырех эндпоинтов с `docs/api/swagger.json`:
      коды, состав полей, пустые тела там, где контракт не описывает `schema`.
      Проверка - e2e-тест на форму ответов и результаты
      `dotnet format --verify-no-changes`, `dotnet build`, `dotnet test`.
