## 1. Схема БД и миграция

- [ ] 1.1 Описать в `src/database/schema.ts` таблицу `users`: `id` (`serial`,
      первичный ключ - контракт требует числовой `id`), `first_name`,
      `second_name`, `login`, `email`, `phone`, `password_hash` - все `notNull`;
      `display_name` и `avatar` - nullable; `created_at`/`updated_at` с
      `defaultNow()`. Уникальные индексы на `login` и `email`. Проверка:
      `npx tsc --noEmit` проходит.
- [ ] 1.2 Описать таблицу `sessions`: `id` (`text`, первичный ключ), `user_id`
      (`integer`, `notNull`, `references(users.id, { onDelete: 'cascade' })`),
      `created_at` (`defaultNow()`), `expires_at` (`timestamp` с таймзоной,
      `notNull`), индекс по `user_id`. Проверка: `npx tsc --noEmit` проходит.
- [ ] 1.3 Сгенерировать первую миграцию `npm run db:generate` и накатить ее
      `npm run db:migrate` на поднятый `docker compose up -d postgres`.
      Проверка: файл появился в `drizzle/`, в базе есть обе таблицы и оба
      уникальных индекса.
- [ ] 1.4 Дописать в `README.md` шаг «поднять базу и накатить миграции» перед
      запуском приложения и `npm run test:e2e`. Проверка: инструкция проходится
      с нуля на чистой базе.

## 2. Пароль и сессия

- [ ] 2.1 Реализовать `PasswordHasher` на `scrypt` из `node:crypto`: хеш в
      формате `scrypt$N$r$p$<salt>$<hash>`, соль 16 байт на пользователя,
      сравнение через `timingSafeEqual`, асинхронная форма `crypto.scrypt`.
      Проверка: unit-тесты на «хеш не равен паролю», «два одинаковых пароля
      дают разные хеши», «проверка своего хеша истинна, чужого - ложна».
- [ ] 2.2 Добавить в `PasswordHasher` проверку по фиктивному хешу для случая
      «пользователь не найден», чтобы время ответа не выдавало занятые логины.
      Проверка: unit-тест на то, что метод отрабатывает и возвращает `false`.
- [ ] 2.3 Реализовать `SessionService`: создание сессии (`randomBytes(32)` в
      `base64url`, `expires_at` из `SESSION_TTL_DAYS`), поиск живой сессии по
      идентификатору, удаление одной сессии, ленивое удаление истекшей при
      обращении. Проверка: unit-тесты с подменой доступа к БД на создание,
      поиск, удаление и на то, что истекшая сессия не находится и удаляется.
- [ ] 2.4 Добавить типизированную конфигурацию: `SESSION_COOKIE_NAME`,
      `SESSION_TTL_DAYS`, `COOKIE_SECURE`, `COOKIE_SAME_SITE`, `CORS_ORIGINS` -
      с валидацией на старте и падением приложения при пустом `CORS_ORIGINS`.
      Отразить их в `.env.example` и `docker-compose.yml`. Проверка: unit-тест
      на отказ стартовать с пустым `CORS_ORIGINS`, приложение поднимается с
      заполненным `.env`.

## 3. Инфраструктура запроса

- [ ] 3.1 Подключить `cookie-parser`, `class-validator` и `class-transformer`;
      включить `cookieParser()` в `src/main.ts`. Проверка: `npm ci` и
      `npm run build` проходят.
- [ ] 3.2 Включить глобальный `ValidationPipe` с `whitelist: true`,
      `forbidNonWhitelisted: true`, `transform: true`. Проверка: e2e - запрос
      с лишним полем `role` в теле получает `400`.
- [ ] 3.3 Реализовать глобальный фильтр исключений, приводящий `4xx`/`5xx` к
      телу `{ reason }`, кроме `401` без тела; сообщения валидации склеиваются
      в один `reason`; необработанная ошибка логируется целиком, а наружу идет
      `500` с обезличенным текстом. Проверка: unit-тесты фильтра на каждый из
      трех случаев.
- [ ] 3.4 Включить CORS со списком origin из `CORS_ORIGINS` и
      `credentials: true`. Проверка: e2e - запрос с разрешенным `Origin` несет
      `Access-Control-Allow-Origin` и `Access-Control-Allow-Credentials`, с
      посторонним - не несет.
- [ ] 3.5 Реализовать `CurrentSessionGuard` через `APP_GUARD`, декоратор
      `@Public()` и декоратор `@CurrentUser()`; пометить `@Public()` health-
      контроллер. Проверка: unit-тесты guard'а на «нет cookie», «сессии нет»,
      «сессия истекла», «сессия живая»; e2e - `GET /api/v2/health` доступен
      гостю.

## 4. Регистрация и вход

- [ ] 4.1 Завести модуль `src/identity-access/` с контроллером `auth`, сервисом
      и DTO `SignUpRequestDto` / `SignInRequestDto`; регулярки `email` и `phone`
      взять из `docs/api/swagger.json`. Проверка: unit-тесты DTO на все случаи
      валидации из спеки (нет поля, кривой email, кривой телефон, пустая строка).
- [ ] 4.2 Реализовать `POST /auth/signup`: создание пользователя с хешем
      пароля, `display_name` и `avatar` пустые, ответ `200` с `{ id }` и
      установкой session cookie. Проверка: e2e на успешную регистрацию с
      последующим `GET /auth/user` по выданной cookie.
- [ ] 4.3 Обработать нарушение уникальности (`23505`) как `400` с `reason`
      `Login already exists` или `Email already exists` - по тому, какой индекс
      нарушен. Проверка: e2e на занятый логин и на занятый email, в обоих
      случаях второй пользователь не создан и cookie не выдана.
- [ ] 4.4 Реализовать `POST /auth/signin`: проверка пароля, создание сессии,
      ответ `200` телом `OK` и установкой cookie; неверные учетные данные -
      `400` с `{ reason: 'Login or password is incorrect' }`, одинаковым для
      неизвестного логина и неверного пароля. Проверка: e2e на успешный вход,
      неверный пароль и несуществующий логин.
- [ ] 4.5 Завершать прежнюю сессию при входе с уже живой cookie. Проверка: e2e - вход вторым пользователем поверх сессии первого делает прежнюю cookie
      неопознаваемой.

## 5. Текущий пользователь и выход

- [ ] 5.1 Реализовать `GET /auth/user`: ответ `200` ровно восемью полями
      контракта, `display_name` и `avatar` как `null`, когда не заданы; хеш
      пароля и служебные поля сессии не отдаются. Проверка: e2e сверяет набор
      ключей ответа с `UserResponse` из `swagger.json` и убеждается, что поля с
      паролем нет.
- [ ] 5.2 Убедиться, что гость получает `401` без тела - без cookie, с cookie
      несуществующей сессии и с cookie истекшей сессии. Проверка: три e2e-теста;
      истекшая сессия готовится записью с прошедшим `expires_at`.
- [ ] 5.3 Реализовать `POST /auth/logout`: удаление сессии, сброс cookie, ответ
      `200` с пустым телом; гостю тоже `200`. Проверка: e2e на выход текущего
      пользователя с последующим `401` по прежней cookie и на выход без cookie.
- [ ] 5.4 Убедиться, что выход не трогает другие сессии того же пользователя.
      Проверка: e2e с двумя агентами supertest - после выхода в одном второй
      продолжает получать `200` на `GET /auth/user`.
- [ ] 5.5 Проверить атрибуты cookie: `HttpOnly`, `Path=/`, `Secure` и
      `SameSite` из конфигурации, а также что сессия не принимается через
      заголовок `Authorization`. Проверка: e2e на заголовок `Set-Cookie` и на
      `401` при передаче идентификатора сессии заголовком.

## 6. Сдача блока

- [ ] 6.1 Прогнать `npm run lint`, `npm run test:ci` и `npm run test:e2e` на
      поднятой базе. Проверка: все три зеленые.
- [ ] 6.2 Проверить контракт по `docs/api/swagger.json`: пути, методы, коды и
      состав полей всех четырех эндпоинтов совпадают. Проверка: расхождений нет;
      если нашлось - это отклонение, оно заводится отдельным change, а не
      правится молча.
- [ ] 6.3 Поднять `docker compose up --build` и пройти сценарий вручную:
      регистрация, вход, перезагрузка страницы фронта с подмененным
      `apiBaseUrl`, выход. Проверка: фронт проходит все четыре шага.
